"""Placeholder data loader for the Crypto · Risk Analysis team.

When your team adds data, import `team_artifacts_dir` from
`dashboard.core.paths` and read from your manifest-declared directory.
See AGENTS.md at the repo root for the full pattern.
"""

from __future__ import annotations

import pandas as pd


def load_placeholder() -> pd.DataFrame:
    return pd.DataFrame()
